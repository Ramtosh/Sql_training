create table rkg(
id int,
Name varchar(20),
Address varchar(20),
Salary int,
)
select*from rkg
insert rkg values(5,'kajal','Noida',23000)

delete from rkg where name= 'ramjeet'

or
delete from rkg where id = 4
